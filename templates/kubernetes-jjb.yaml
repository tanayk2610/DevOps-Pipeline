- job:
    name: kubernetes
    workspace: /var/lib/jenkins/jobs/kubernetes/workspace
    description: "create kubernetes cluster"
    builders:
      - shell: "sudo ansible-playbook /files/kubernetes.yml"
