- job:
    name: checkbox
    description: "automatically build checkbox"
    builders:
      - shell: 'cd /checkbox.io/server-side/site/ && npm install'
    publishers:
      - trigger:
          project: ansible-checkbox
